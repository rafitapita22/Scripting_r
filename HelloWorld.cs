using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Al empezar va a saludar
      print("Hola");  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
